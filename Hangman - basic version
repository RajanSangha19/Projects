def printletters(list,nlist):
    # Pring the characters in the list
    for i in range(nlist):
        print(list[i]," ",end="")
    print()    
# PASTE YOUR FILLGUESS FUNCTION BELOW
def fillguess(word, guess, length, letter):
  numletters = 0
  for i in range(length):
    if letter == word[i]:
      guess[i]=letter
      numletters+=1
  return numletters
#################### Main Program #################################
numguessed=0
lives=8
# Read in word to be guessed
word=input("Enter word for the other player to guess :")
# Now clear the screen
for i in range(40):
    print()
# Store the number of letters in the answer
length=len(word)
# Create an array for the solution fill with underscores
guess=["_"]*length
# ADD THE REST OF THE CODE FOR THE FLOW CHART IN YOUR DESIGN HERE
while lives > 0 and numguessed<length:
  printletters(guess,length)
  letter=input("what letter do you think it is?")
  num = fillguess(word, guess, length, letter)
  if num>0:
    numguessed = numguessed + num
  else:
    lives = lives - 1
if lives > 0:
  print("You are a winner")
  print(f"The word is {word}")
else:
  print("You have lost")
  print(f"The word is {word}")
